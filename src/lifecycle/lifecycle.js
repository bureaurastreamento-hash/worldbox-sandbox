// Fatia 5: envelhecimento por tick, morte (fome crítica, combate, idade) e
// reprodução simples entre agentes da mesma vila. Consome needs.js (fome
// crítica) e escreve na população da vila (village.js).

// TODO (fatia 5): ageAgent(agent, dt), checkDeath(agent), tryReproduce(agentA, agentB, village)
