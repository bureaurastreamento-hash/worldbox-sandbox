// Fatia 4: estoque comunitário e cálculo de demanda por recurso (estoque
// baixo -> demanda alta -> influencia o utility score de ações que suprem
// esse recurso, para todos os moradores).

// TODO (fatia 4): addStock(village, type, amount), computeDemand(village) -> { [resource]: 0-1 }
